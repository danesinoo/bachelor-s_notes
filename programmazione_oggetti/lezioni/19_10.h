/*
 * Variabili statiche -> sono quelle variabili che sono allocate nella memoria
 *                          statica. Il loro lifetime dura dall'inizio del
 *                          programma fino alla fine dello stesso.
 *
 * Locali di una funzione:
 *      Variabili locali
 *
 *      Parametri
 *
 *      Temporanei -> nel caso di ritorno di una funzione
 *
 * Nella programmazione a oggetti il distruttore è quel metodo che deve essere
 * chiamato quando un oggetto deve essere deallocato. -> avviene in modo
 * automatico se le variabili sono automatiche, per le variabili statiche il
 * distruttore è chiamato alla fine del programma.
 *
